# CampusFlow 静态 QA 报告

检查范围：`index.html`、`app.js`、`styles.css`、`manifest.json`、`sw.js`。检查日期：2026-08-25。当前 `node --check app.js` 通过；本报告记录本轮实现与回归验证结果。

## 已通过

- 已有九个可达入口：总览、计划、课表、日历、习惯、笔记、统计、专注、设置。
- JSON/CSV 导出、JSON 导入、重复选择文件重置、课程/事件/任务/习惯/笔记删除按钮均已接入。
- 课程保存会提示同日同节冲突；事件结束时间早于开始时间会阻止保存。
- `index.html` 已引用 manifest；`app.js` 在 HTTP(S) 下注册 Service Worker；新增视图已有响应式 CSS。
- 360px 窄屏搜索框可通过搜索图标展开并输入；弹窗打开后首焦点落到表单控件，Tab 会循环，Escape 关闭后焦点返回触发按钮。
- 模板中的用户输入主要经过 `escapeHtml`；语法检查通过。
- 课表已按当前学期周次过滤 `1-16`、单周/双周等规则，并按 `duration` 占用连续节次；跳转到第 17 周后，1–16 周课程不会显示。
- 学期周次以 `profile.startDate` 所在周一为基准计算，而不是自然年周次；课表与总览使用同一学期周数。
- 编辑日历事件时会根据原事件的 `type` 设置下拉框选项，保存后不会意外重置为“考试”。
- JSON 导入会对 `tasks`、`courses` 及其他数组字段逐项回退旧数据；导入文件输入值会重置，因此可重复选择同一文件。
- Service Worker 缓存键已升级为 `campusflow-v1.0.1`，激活时会清理旧版本缓存，避免长期命中旧静态资源。
- Service Worker 激活时只清理 `campusflow-*` 缓存，不会删除同一域名下其他 GitHub Pages 项目的离线缓存。

## 高优先级（本轮已修复）

| 编号 | 现象/复现 | 影响 | 建议 |
| --- | --- | --- | --- |
| H1 | 课表渲染曾未按 `duration` 跨行，也未按 `weeks`/单双周过滤。 | 连续多节课只占一格；第 17 周以后仍显示 1–16 周课程。 | 已修复：按当前学期周次筛选，并在每个节次匹配课程占用区间。 |
| H2 | 周次曾按自然年计算。 | 学期周次核心信息错误。 | 已修复：以开学日期所在周一为基准计算学期周。 |
| H3 | 编辑事件曾把类型重置为“考试”。 | 颜色/分类会被意外改写。 | 已修复：编辑表单为原类型 option 设置 `selected`。 |
| H4 | 导入缺失字段曾覆盖为 `undefined`。 | 后续渲染可能抛异常。 | 已修复：逐字段回退旧数据并保留 profile/daily/focus/settings 的已有字段。 |
| H5 | Service Worker 缓存键曾固定为旧版本。 | 客户端可能长期读取旧静态资源。 | 已修复：缓存版本升级到 `campusflow-v1.0.0`，激活时清理旧缓存。 |

## 中优先级（数据正确性/体验）

| 编号 | 现象/复现 | 建议 |
| --- | --- | --- |
| M1 | 已改为扫描历史日志计算每个习惯的最长连续段；首页仍显示当前连续天数。 | — |
| M2 | 已改为渲染“前 7 天 + 本周 7 天”的连续 14 格。 | — |
| M3 | 统计已按当前自定义专注时长计算；尚未记录每一轮不同的历史时长。 | 后续可增加 focusSessions 日志，精确还原每轮时长。 |
| M4 | 全局搜索在计划页过滤任务，在笔记页过滤标题和正文；其他页面输入搜索词会回到计划页。 | 后续可增加课程/事件的统一结果面板。 |
| M5 | “未来 7 天”已限定时间窗；即将到来列表中的事件会携带 id，点击可编辑指定事件。日历格点击仍默认打开当天第一条事件。 | 后续可做日期详情抽屉，展示同日全部事件。 |
| M6 | 课程冲突已按连续节次、周次范围及单双周交集检测；学期日期表单仍可继续加强严格校验。 | 增加日期格式/结束日期与周数的可视化校验。 |
| M7 | JSON 导入确认后直接覆盖，没有导入差异预览/撤销；导入失败时旧数据仍在但用户无法回滚已确认覆盖。 | 写入前保存内存快照并提供撤销条。 |

## 低优先级/增强项

- 已提供 `favicon.svg` 并接入 `manifest.icons`，安装到桌面可显示基础图标。
- `daily` 记录是单一全局对象，不按日期保存，无法查看历史饮水/心情/睡眠。
- 课程编辑文案提示冲突但允许保存；应在冲突提示中列出重叠课程并提供“仍保存”明确选择。
- `note-item` 仍依赖 div 点击，键盘可访问性不足；可后续改为原生按钮或补充键盘语义。
- 首页 breadcrumb 的“2026 · 2027”是静态文案，修改学年设置后不会同步。
