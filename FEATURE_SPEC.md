# 大二计划工具：功能与信息架构规格

> 面向即将进入大二的学生。目标是把“学期目标 → 课表 → 日历/待办 → 每日执行 → 复盘统计”串成一条低摩擦的闭环。本文是实现与验收依据，不限定具体技术栈。

## 1. 产品原则与边界

- **一眼知道今天做什么**：打开首页即可看到下一节课、待办、习惯和当天进度。
- **输入一次、处处复用**：课程、任务、考试和事件都可以互相链接，避免重复录入。
- **可恢复、可迁移**：所有编辑支持撤销/恢复；数据可导出为 JSON/CSV/ICS，换设备不丢失。
- **离线优先**：无网络时读取与编辑仍可用，恢复网络后再同步（若后续接入云同步）。
- **渐进复杂度**：首次使用只要求设置学期和课程；高级功能（循环、统计、导入）按需展开。

## 2. 全局信息架构

桌面端侧栏与移动端底部导航使用同一套一级入口：

1. **总览**（Dashboard）
2. **课表**（Timetable）
3. **日历**（Calendar）
4. **计划**（Tasks & Goals）
5. **习惯**（Habits / Daily）
6. **统计**（Insights）
7. **设置**（Settings）

全局操作：`+ 新建`（任务/事件/课程/习惯）、搜索（`Ctrl/Cmd + K`）、通知中心、撤销/重做、数据导入导出。

## 3. 首次使用与学期设置

### 3.1 首次引导（3 步以内）

1. 选择学年、学期、开学日期、周数（默认 20 周）及所在时区。
2. 添加课程（支持跳过，之后可从课表导入）。
3. 选择关注的习惯模板（早起、运动、英语、复习等，可全不选）。

引导可跳过；每一步都能返回；刷新/关闭后恢复上次进度。没有数据时首页显示明确的空状态与“开始设置”按钮。

### 3.2 学期实体

- `semesterId, name, academicYear, term, startDate, endDate, totalWeeks, timezone`
- 节假日/调休列表（日期、名称、是否停课）。
- 可归档旧学期；归档后只读但仍可查看统计和导出。

## 4. 总览（Dashboard）

### 4.1 信息层级

- 顶部：日期、学期周次（如“第 5 周 / 共 20 周”）、今日完成率、快捷新建。
- **下一节课卡片**：课程、时间、地点、教师、剩余时间；无课时显示“今日无课”。
- **今日时间线**：课程、事件、任务按时间排序；已完成项折叠。
- **待办摘要**：逾期、今日到期、未来 7 天各一组，显示优先级和所属课程。
- **习惯摘要**：今日打卡进度与连续天数。
- **本周目标/进度**：目标完成环形图；提供“开始专注”入口（可选番茄钟）。

### 4.2 交互

- 卡片可拖拽排序，排序持久化；窄屏自动单列。
- 快速完成任务/习惯无需离开首页；误操作提供 5 秒撤销条。
- 点击课程进入课程详情；点击任务进入编辑抽屉；点击日期跳转日历。

## 5. 课表系统（重点）

### 5.1 视图与导航

- 周视图为默认：周一至周日 × 节次/时间轴；显示当前时间线。
- 日视图：移动端默认，支持左右滑动切换日期。
- 月视图：查看考试、调课和截止日期，不强制显示每节课。
- 顶部显示“第 N 周”，支持上一周/下一周、回到本周、指定周跳转。
- 可切换“全部课程 / 某课程 / 某地点”筛选；颜色由课程继承。

### 5.2 课程与上课实例

课程（长期属性）：`id, name, code, teacher, room, color, credits, assessment, notes`。

上课规则（循环属性）：

- 星期（1–7）、开始/结束时间或节次、起止周。
- 周类型：每周、单周、双周、自定义周集合。
- 校历例外：停课、调课到其他日期/时间/地点、补课。
- 提醒：课前 5/15/30 分钟，可按课程覆盖默认值。

渲染时先生成实例，再应用例外；例外必须带 `sourceRuleId`，便于撤销和重新计算。

### 5.3 新建/编辑课程

- 表单实时校验：结束时间晚于开始时间；周次在学期范围；地点/教师可选。
- 保存前显示冲突预览（同一时段课程、调休冲突），允许“仍然保存”并记录冲突。
- 编辑循环课程时明确选择“仅本次 / 从本次起 / 全部”。
- 删除同样提供范围选择与撤销，不直接破坏整学期数据。

### 5.4 课表更新机制

- **手动更新**：编辑后显示“未保存更改”标记；保存成功给出时间戳与变更摘要。
- **批量导入**：支持 CSV、XLSX（列映射向导）、ICS；预览新增/修改/删除，逐条或全选确认。
- **差异合并**：以稳定键（课程代码 + 星期 + 时间 + 周次）匹配；无法匹配的行进入“待确认”。绝不静默覆盖用户的备注、颜色和例外。
- **版本与回滚**：每次导入生成版本快照，保留最近 10 个；支持查看差异、回滚到任一快照。
- **重复导入幂等**：同一文件/同一稳定键再次导入不产生重复课程。
- **失败处理**：部分行失败时保留成功行并列出错误行、原因和修复建议；可下载错误报告。
- 可选“更新中心”：显示当前数据版本、最近同步时间、待处理导入和应用更新说明。

### 5.5 课表导出

- 导出当前学期为 CSV/XLSX（便于打印/修改）或 ICS（便于系统日历订阅）。
- ICS 需包含时区、重复规则、例外日期、提醒；导出前选择全部课程或筛选结果。

## 6. 日历与事件

- 月/周/日三视图；事件类型：考试、作业截止、社团、个人、调课。
- 事件字段：标题、开始/结束、全天、地点、描述、重复、提醒、颜色、关联课程/任务。
- 拖拽移动、拉伸调整时长；移动端采用底部抽屉编辑。
- 从任务设置截止日期时自动生成日历标记（可关闭）；删除任务不默认删除已存在的事件。
- 重复事件支持例外（跳过/仅改本次）；跨时区显示以学期时区为准并注明本地时间。

## 7. 计划、待办与目标

### 7.1 任务

- 收件箱、今日、未来 7 天、项目/课程、已完成、回收站视图。
- 字段：标题、描述、状态、优先级（低/中/高/紧急）、截止日期/时间、预计时长、标签、课程、目标、子任务、附件/链接。
- 支持拖拽排序、批量完成/改期/归档；逾期任务醒目标记但不弹窗骚扰。
- 重复任务（每天/每周/自定义）可选择完成后生成下一次；避免跨时区重复漂移。

### 7.2 学期目标

- 目标 → 里程碑 → 任务三级结构；目标有截止日期、衡量指标、进度和复盘备注。
- 模板：绩点、证书、运动、作品集、语言学习；可自定义。
- 周复盘提示：本周完成、阻塞、下周承诺。

### 7.3 专注计时（可选）

- 25/5、50/10、自定义；计时结束记录到关联任务/课程的学习时长。
- 页面刷新或切换标签不丢失计时状态；暂停/恢复明确显示。

## 8. 习惯与日常

- 习惯字段：名称、频率（每日/每周次数/指定日）、目标量与单位、提醒、颜色、开始日期、归档状态。
- 今日清单支持一键打卡、数量输入、补打卡（需标记为补录）；连续天数和完成率可见。
- 日常记录（可选）：睡眠、心情、运动、饮水、读书；默认关闭，用户主动开启。
- 允许设置“休息日”，休息日不打断连续统计；删除习惯保留历史数据。

## 9. 统计与复盘（Insights）

- 时间范围：本周、本月、学期、自定义；所有图表可导出 PNG/CSV。
- 指标：任务完成率/逾期率、习惯完成率/最长连续、专注时长、课程出勤（手动登记）、目标进度、考试倒计时。
- 图表：周趋势折线、课程/项目分布柱状、日历热力图、时间段分布。
- 统计口径在图表旁注明（例如“专注时长仅含已完成计时”）；无数据显示空状态而非 0 误导。
- 周复盘摘要可编辑并导出为 Markdown/PDF（若实现 PDF）。

## 10. 数据、同步与更新

- 本地持久化建议使用 IndexedDB（带 schemaVersion）；关键设置可用 localStorage 缓存。
- 导入文件先解析为临时数据，用户确认后事务式写入；失败整批可回滚。
- JSON 备份包含 `schemaVersion`, `exportedAt`, `timezone`, `semester`, `courses`, `classRules`, `exceptions`, `events`, `tasks`, `habits`, `goals`, `logs`。
- 导入旧版本时执行显式迁移并保留原文件；迁移失败不改变现有数据。
- 多设备同步（若后续接入）采用 `updatedAt` + 稳定 ID；冲突显示对比，不以最后写入静默覆盖。
- Service Worker 更新采用“提示可更新 → 用户确认 → 刷新”，避免编辑中强制刷新；缓存失败自动回退到上一版本。

## 11. 设置、主题与可访问性

- 主题：浅色/深色/跟随系统；强调色、课程颜色、字号（小/标准/大）、紧凑/舒适密度。
- 通知：课程/任务/习惯分别开关；支持浏览器通知与免打扰时段；通知权限被拒绝时给出设置指引。
- 语言：简体中文为默认，文案集中管理，日期/周起始日可配置。
- 键盘：`N` 新建、`T` 回到今天、`[`/`]` 切周、`Ctrl/Cmd+K` 搜索；所有操作有可见焦点。
- 对比度满足 WCAG AA；颜色不是唯一信息载体；触控目标 ≥44px；支持减少动效。

## 12. 移动端体验

- 断点：窄屏单列；课表默认日视图，周视图可横向滚动且首列/时间轴固定。
- 底部导航保留 4 个高频入口（总览、课表、计划、更多），其余在“更多”中。
- `+` 浮动按钮打开操作菜单；表单使用底部抽屉，自动避开键盘和安全区。
- 下拉刷新数据；左右滑动切换周/日；长按课程/任务进入快捷操作。
- PWA：可安装、离线启动、网络恢复提示；不要求强制登录。
- 大数据量时按可见日期渲染，避免滚动卡顿；空状态和错误状态可在小屏完整阅读。

## 13. 错误与边界验收清单

- 首次启动、无课程、无任务、无网络、存储空间不足均有可操作空/错状态。
- 日期校验覆盖闰年、跨年学期、夏令时、跨午夜课程、周起始日配置。
- 导入处理空行、重复行、非法日期、未知列、编码（UTF-8/GBK）、超大文件；不因一行错误丢失整批。
- 冲突检测涵盖同一课程重复、不同课程重叠、调课与节假日重叠；冲突可忽略但必须留痕。
- 所有破坏性操作有确认、影响范围说明和撤销；回收站支持单项恢复/永久删除。
- 刷新、关闭标签、浏览器后退不会意外丢失未保存编辑；离开表单前提示。
- 时钟变化/系统休眠后计时器、提醒和“今天”正确恢复。
- 长标题、超长备注、极端数字、特殊字符和中文输入法不会导致布局溢出或提交异常。

## 14. 最小可交付与完成定义

**MVP**：学期设置、课程/循环课表（周/日视图）、任务、日历事件、习惯打卡、JSON 备份/恢复、浅/深色主题、响应式布局。

**完善版**：CSV/XLSX/ICS 导入导出与差异预览、调课例外、版本回滚、目标与周复盘、统计图表、通知、PWA 离线、键盘/无障碍支持。

每个功能完成需满足：

1. 空/加载/错误状态齐全；
2. 表单校验、撤销或可恢复路径齐全；
3. 刷新和离线场景数据不丢；
4. 移动端（宽度 360px）与桌面端均可操作；
5. 至少覆盖一条正常流程、一条边界流程和一次导入/导出回归测试。

